// import { mount } from '@vue/test-utils'

// test("login when demo", () => {
//   expect(userService.login({ fullName: "Guest Guest", password: "Guest Guest" })).not.toBeNull();
// });

// test("login with non-existing user", () => {
//   expect(userService.login({ fullName: "Shani", password: "123" })).toBeNull();
// });